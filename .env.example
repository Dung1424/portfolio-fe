# Open the app at http://localhost:8088 (gateway). This compose file does not publish :3000 on the host.
# NUXT_PUBLIC_API_BASE=http://localhost:8088
# NUXT_PUBLIC_CHAT_API=http://localhost:8088
# NUXT_PUBLIC_WEBRTC_ICE_SERVERS=[{"urls":"stun:stun.l.google.com:19302"}]
# Chat REST (Mongo) phai cung host voi gateway hoac :3010 Node; neu CHAT_API = Laravel :8000 thi /conversations/.../pin se 404.
#
# Laravel only (no gateway for API):
# NUXT_PUBLIC_API_BASE=http://127.0.0.1:8000
